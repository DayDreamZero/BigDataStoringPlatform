import * as constants from './constants';

const defaultState = {
  //value
};

export default(state=defaultState, action) => {
  // if(action.type == constants.){
  //   return newState;
  // }
  return state;
}